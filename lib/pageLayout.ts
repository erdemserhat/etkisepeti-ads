/**
 * Standard content frame: matches header, main, and category pages.
 * Implemented as `.page-frame` in globals.css for identical edges with the fixed header.
 */
export const PAGE_CONTENT_GUTTER = "page-frame";
